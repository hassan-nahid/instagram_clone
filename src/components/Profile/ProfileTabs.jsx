import { Flex } from "@chakra-ui/react";

const ProfileTabs = () => {
    return (
        <Flex w={"full"} justifyContent={"center"}>
            
        </Flex>
    );
};

export default ProfileTabs;